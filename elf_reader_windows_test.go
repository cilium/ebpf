package ebpf

import "testing"

func TestWindowsELFSections(t *testing.T) {
	// TODO(windows): Iterate through all sections
	// and make sure the types do match.
	// XXX: What if an extension isn't loaded?
}
